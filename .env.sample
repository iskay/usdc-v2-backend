# Application Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=debug

# Database Configuration
# For Docker Compose, this will be automatically constructed from POSTGRES_* variables
# For local development, set this directly
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/usdc_v2_backend

# Postgres Configuration (used by Docker Compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=usdc_v2_backend
POSTGRES_PORT=5432

# Redis Configuration
# For Docker Compose, use: redis://redis:6379
# For local development, use: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# CORS Configuration
# Comma-separated list of allowed origins
# Example: http://localhost:5173,http://localhost:3000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Chain Registry Configuration
# The backend uses a built-in chain registry with default RPC URLs for each chain.
# To override RPC URLs for specific chains, provide a custom chain registry JSON file.
# See docs/operations.md for the chain registry format.
# CHAIN_REGISTRY_PATH=./config/chains.json

# Optional: Chain Polling Configuration Override
# JSON string with chain-specific polling configurations
# Example: {"sepolia-testnet":{"maxDurationMin":15,"blockWindowBackscan":10,"pollIntervalMs":3000}}
# CHAIN_POLLING_CONFIGS={"sepolia-testnet":{"maxDurationMin":15,"blockWindowBackscan":10,"pollIntervalMs":3000}}

# Note: EVM_RPC_URLS and TENDERMINT_RPC_URLS environment variables are parsed but not currently used.
# RPC URLs are configured per-chain in the chain registry. To customize RPC URLs, use CHAIN_REGISTRY_PATH.